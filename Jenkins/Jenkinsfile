
pipeline {
  agent any
  options {
  buildDiscarder logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '', numToKeepStr: '5')
  }
  stages {
    stage('Test') {
      steps {
        sh 'echo "test is seccesful"'
      }
    }
    stage('Build static') {
      steps{
        dir ('client') {
          sh 'docker build -t front:latest . '
          sh 'docker run --env_file /opt/jenkins/client_env -v ./date:/build front:latest -d'
        }
      }
    }
    stage('Build backend') {
      steps{
        dir ('server') {
          sh 'docker build -t back:latest .'  
          sh 'docker run --env_file /opt/jenkins/server_env -v ./date:/dist back:latest -d'
        }
      }
    }
/*
    stage('Deploy to stage') {
      steps {
        dir ('docker') {
          sh 'docker compose up -d'
        }
      }
    } 
    */
  }
}


