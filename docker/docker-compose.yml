version: "3.8"
services:
  front:
    image: front
    volumes:
      - "/opt/nginx/data:/build"



  web:
    image: nginx
    ports:
     - "80:80"
    volumes:
     - "/opt/nginx/data:/usr/share/nginx/html"
    depends_on:
     - front
  
