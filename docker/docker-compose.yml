version: "3.8"
services:
  front:
    image: front
    volumes:
      - "/opt/data:/build"



  web:
    image: nginx:stable-alpine3.17
    ports:
     - "80:80"
    volumes:
     - "/opt/data:/var/www/html"
    depends_on:
     - front
  
